pub fn annotate(minefield: &[&str]) -> Vec<String> {
    let solution: Vec<String> = Vec::new();
    for i in 0..minefield.len() {
        for (index, element) in minefield[i].char_indices() {
            
        }
    }
    solution
}
